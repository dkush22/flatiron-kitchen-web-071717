<%= form_for @recipe do |f|%>
<%=f.label :name %>
<%=f.text_field :name %>
<% @ingredients.each do |ingredient| %>
<input type="checkbox" id="<%= ingredient.name %>" name="ingredient[]" value="<%= ingredient.id %>" /> <%= ingredient.name %>
<% end %>
<%= f.submit %>
<% end %>